package com.tiwater.ticos;

/**
 * Enum representing storage modes for Ticos Client.
 */
public enum SaveMode {
    /**
     * Store data in internal storage (user's home directory)
     */
    INTERNAL,
    
    /**
     * Store data on external storage (TF card)
     */
    EXTERNAL
}
